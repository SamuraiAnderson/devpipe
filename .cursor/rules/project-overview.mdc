---
description: RedPyMake 项目概览与顶层约定
alwaysApply: true
---

# RedPyMake 项目概览

**定位：** 跨运行环境（本地 / SSH / ADB / 串口）的 Python 自动化库。核心业务是**会话**（连接与工作目录）、**命令**、**文件传输**、**日志与等待**，以及作为增量优化辅助的**过时判断** `rpm.stale`。

**推荐调用方式：** `import redpymake as rpm`，通过工厂 `rpm.local()` / `rpm.ssh(...)` / `rpm.adb(...)` / `rpm.serial(...)` 拿到 `Session`。

## 目录结构

- `src/redpymake/` — 核心包（PEP 561：包内 `py.typed`）
  - `__init__.py` — 顶层公开 API（`__all__` 明确定义）
  - `_session.py` — `Session` 抽象基类：`run`/`at`/`path`/`wait`/`push`/`pull`/`copy`
  - `_local.py` / `_ssh.py` / `_adb.py` / `_serial.py` — 各平台实现
  - `_factory.py` — `local/ssh/adb/serial` 工厂 + 进程级默认 local
  - `_command.py` / `_transfer.py` / `_path.py` — 数据对象
  - `_logs.py` — 日志缓冲、tag、cursor、wait、订阅
  - `_stale.py` — 顶级函数 `stale(...) -> bool`
  - `exceptions.py` — 统一异常层次（`RedPyMakeError` 根）
- `tests/` — 不依赖网络/真实设备的单元测试（集成测试用 `@pytest.mark.integration`）
- `doc/core-lib-requirements.md` — **规范来源**：任何行为分歧以此文档为准

## 约定

- Python ≥ 3.10；核心库不依赖 Streamlit / NumPy / SciPy / pydub。
- 命名：类 `PascalCase`；函数/变量 `snake_case`；实现细节前缀 `_`。
- 结构化类型用 `dataclass`（`frozen=True` 优先）。
- 中文注释与 docstring；与用户交流使用中文。
- 不得抛出 Paramiko / RuntimeError 之类第三方或通用异常；一律包装为 `RedPyMakeError` 子类。
- 库自身不调用 `logging.basicConfig()`；日志通过 `SessionLogs` 收集。

## 导入约定

- 库内使用相对导入：`from ._logs import LogBuffer`。
- 外部使用者：`import redpymake as rpm`；不允许从 `redpymake._session` 等下划线模块 import。
- 可选平台（SSH / 串口）通过 `__init__.__getattr__` 惰性暴露，避免强制导入 paramiko/pyserial。
