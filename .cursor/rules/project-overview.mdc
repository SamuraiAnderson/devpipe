---
description: RedPyMake 项目总览 — 架构、目录、导入与命名约定
alwaysApply: true
---

# RedPyMake 项目概览

**功能定位：** RedPyMake 是一套以 Python 为任务脚本的跨平台自动化框架，并在同一套模型上提供 Web 可视化：用统一抽象管理「会话连接、文件与远端资源、脚本化任务」，在界面中浏览脚本、静态展示脚本中的终端/服务拓扑，并运行脚本、分源查看日志。

**可视化（现状）：** 基于 Python AST 的控制器/服务实例卡片与多 Tab 日志；DAG/流程图类能力为规划方向，见 `doc/TODO.md`。

**核心执行模型：** 在不同平台（本地、Linux/SSH、Android/ADB、串口）上执行命令、传输文件，并提供 Make 风格增量执行。

**日志分流（概要）：** UI 层 `ui.*`（除 `ui.services.script_service`）仅终端输出、不进 Web 日志缓冲；脚本服务与任务脚本日志进「脚本」Tab；`client.*` 对端输出进各会话 Tab。不设「应用」Tab；细则见 `.cursor/rules/ui-conventions.mdc`「日志分流」。

## 目录结构

- `src/` — 核心库：`BaseControl` 基类及各平台实现、`UFile`、`make_style`
- `ui/` — Streamlit Web 界面：`components/`(面板组件)、`services/`(业务服务)
- `example/` — 示例/测试脚本，通过 `.env` + `argparse` 配置
- `doc/` — 设计文档

## 导入约定

- `src/` 内部使用**相对导入**：`from .BaseControl import *`
- `example/`、`ui/` 通过 `sys.path.insert(0, PROJECT_ROOT)` 后 `import src`

## 命名约定

- 类：`PascalCase`（`BaseControl`、`AdbCnet`、`LocalHost`）
- 函数 / 变量：`snake_case`
- 私有成员：`_leading_underscore`（`_host`、`_stream_and_tee`）
- 使用 `dataclass` 和 `Enum` 做结构化类型定义

## 语言

- 代码注释和 docstring 使用**中文**
- 与用户交流使用**中文**
